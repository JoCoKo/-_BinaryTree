#include "Tree.h"

/*Node::Node(int inp)
{
	value = inp;
}

/*void Node::add(int inp)
{
	if (value != inp)
		if (inp < value)
		{
			if (left != NULL) //Go left
				left->add(inp);
			else
				left = &Node(inp);
		}
		else
			if (right != NULL) //Go right
				right->add(inp);
			else
				right = &Node(inp);

}*/

Node::~Node()
{
	//dtor
}
